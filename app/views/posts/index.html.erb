<div class="individual-header text-center">投稿一覧</div>    
 <div class="content">
    <% @posts.each do |post| %>
        <div class="row justify-content-center">
      <%= render "shared/post", post: post %>
        </div>
    <% end %>
  </div>  
